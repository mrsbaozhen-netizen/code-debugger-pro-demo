# Code Debugger Pro — Live Demo (UI)

Static demo for [Codester](https://www.codester.com) listing.

## What is published here

| Included | Not included (buyer deploys) |
|----------|-------------------------------|
| `index.html` | `api.php` |
| `documentation/index.html` | `proxy/server.js` |

Proxy will show **unreachable** on this static host — that is expected. The link **Proxy unreachable — open setup guide →** and footer **Documentation** must work; that is why `documentation/` is included.

Do not commit API keys.

After push, enable **GitHub Pages**: Repository → Settings → Pages → Branch `main` / folder `/ (root)`.

- App: `https://<username>.github.io/code-debugger-pro-demo/`
- Docs: `https://<username>.github.io/code-debugger-pro-demo/documentation/index.html`
