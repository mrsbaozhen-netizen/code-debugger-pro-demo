# Code Debugger Pro — Live Demo (UI)

Static demo for [Codester](https://www.codester.com) listing.

- **UI only** — PHP proxy (`api.php`) runs on the buyer's server.
- Do not commit API keys.

After push, enable **GitHub Pages**: Repository → Settings → Pages → Branch `main` / folder `/ (root)`.

Demo URL: `https://<username>.github.io/code-debugger-pro-demo/`
